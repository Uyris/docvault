# Only needed when the backend is deployed at a DIFFERENT origin than the
# frontend (e.g. frontend on Vercel, backend on Railway). Leave empty when the
# dev proxy or nginx (docker-compose) serves /api on the same origin.
# VITE_API_BASE=https://your-backend.example.com
